class Solution:
    def getHappyString(self, n: int, k: int) -> str:
        set_ = ['a', 'b', 'c']
        # s[i] != s[i + 1] for all values of i from 1 to s.length - 1 (string is 1-indexed)
        # generate strings using n

        # fusionne le prochain
        n2 = set_[0] + set_[1]
        strings = []
        while True:
            for s in set_:
                
            
        curr_str = []
        
        # for n = 3 ["aba", "abc", "aca", "acb", "bab", "bac", "bca", "bcb", "cab", "cac", "cba", "cbc"]
        # check using k
        
        
        return ""